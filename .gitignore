# Local Python environments and caches
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local agent/runtime metadata
.agents/
.codex/

# Reproducible upstream clones. See README.md for required URLs and commits.
upstream/

# Large vendor document/source bundles downloaded for local review.
# Commit the review notes/manifests instead of raw archives and extracted trees.
docs/vendor/

# Large local manuals/CAD asset bundles.
# Keep these on the robot/workstation; do not push raw assets to GitHub.
docs/assets/
*.STEP
*.step
*.STP
*.stp
*.STL
*.stl

# ROS and build outputs
build/
install/
log/
*.egg-info/

# Runtime artifacts
*.bag
*.db3
*.log

# Accidental shell redirection artifact observed in this workspace
/bash

# Accidental CLI argument artifacts observed in this workspace
/--joint
/--left-can
/--left-delta
/--mode
/--right-can
/--right-delta
/--speed

# OS/editor noise
.DS_Store
__MACOSX/
._*
*~

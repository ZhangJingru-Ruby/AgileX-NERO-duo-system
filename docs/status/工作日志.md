# 工作日志

## 2026-06-24

- 建立 NERO 部署规则和资料基线，明确裸臂优先、灵巧手后置、每步记录证据并修正路线。
- 完成 Ubuntu 20.04 主机环境盘点，确认 ROS2/colcon/ethtool/python-can/pyAgxArm 缺口，并修正环境检查脚本以暴露 Conda Python 风险。
- 克隆并审阅 `agx_arm_ros`、`pyAgxArm`、`piper_ros`，确认默认路线为 ROS2 Humble，而不是 ROS Noetic。
- 选定并落地“Ubuntu 20.04 主机 SDK/CAN + Docker Ubuntu 22.04 ROS2 Humble”混合方案，完成 Docker、`ethtool`、SDK venv、ROS 工作区构建。
- 完成 NERO 裸臂 URDF/xacro/check_urdf/robot_state_publisher/RViz GUI 验证；完成首次上电 Web 只读观察，并把现场状态修正为双臂独立控制器/网络身份。

关键命令：

```bash
bash scripts/check_environment.sh
sudo apt-get update
sudo apt-get install -y docker.io ethtool python3.8-venv
bash scripts/setup_host_sdk_venv.sh
bash scripts/build_humble_container.sh
git clone -b ros2 --recurse-submodules https://github.com/agilexrobotics/agx_arm_ros.git /home/lv-robotics/agx_arm_ws/src/agx_arm_ros
git clone https://github.com/agilexrobotics/pyAgxArm.git /home/lv-robotics/agx_arm_ws/src/pyAgxArm
bash scripts/run_humble_container.sh bash -lc 'cd /root/agx_arm_ws/src/pyAgxArm && pip3 install .'
bash scripts/run_humble_container.sh bash -lc 'cd /root/agx_arm_ws && colcon build'
bash scripts/run_humble_container.sh --allow-xhost ros2 launch agx_arm_description display.launch.py arm_type:=nero effector_type:=none gui:=false control:=true
xhost -local:root
```

## 2026-06-25

- 完成双臂 Web 身份：Arm A/Arm B 热点分别记录为 `agx-7ax-armA`、`agx-7ax-armB`，并确定 `can_arm_a`/`can_arm_b` 与 `arm_a`/`arm_b` 命名。
- 完成双 USB-CAN 映射与激活：Arm A 为 `1-5:1.0`，Arm B 为 `1-11:1.0`，两路 CAN 均为 UP、ERROR-ACTIVE、`1000000` bitrate。
- 完成 S7/S8 只读链路：SDK 只读通过，双臂 ROS 反馈 topic 与 RViz follow 通过，关节反馈约 `200 Hz` 且无错误/限位/通信异常。
- 完成 S9 配置复核：确认裸臂、空载/无负载、`effector_type:=none`、TCP 偏移为零；处理 Arm A CAN 丢帧问题，并通过 USB-CAN 重插与重新激活恢复。
- 完成 S10 首次低速运动闭环：Arm A 和 Arm B 均完成 Web、SDK、ROS 单关节低速验证，动作后 ROS 快照干净，最终控制源审计无活动 NERO Docker/主机控制进程。
- 准备 S11 双臂实验基线：补齐测量记录、静态 TF、rosbag/logging 模板，下一步进入 `lab_world` 与双臂 base transform 实测。

关键命令：

```bash
bash scripts/find_can_ports.sh
bash scripts/activate_can.sh can_arm_a 1000000 "1-5:1.0"
bash scripts/activate_can.sh can_arm_b 1000000 "1-11:1.0"
ip -details link show can_arm_a
ip -details link show can_arm_b
timeout 5s candump -tz can_arm_a
timeout 5s candump -tz can_arm_b
bash scripts/run_humble_container.sh bash /workspace/nero/scripts/launch_dual_ros_readonly.sh
NERO_CONTAINER_NAME=nero-humble-snapshot bash scripts/run_humble_container.sh bash /workspace/nero/scripts/snapshot_ros_readonly_state.sh
.venv/nero-sdk/bin/python examples/nero_sdk_single_joint_step.py --channel can_arm_a --firmware v112 --joint 1 --delta-deg 2 --speed-percent 5
.venv/nero-sdk/bin/python examples/nero_sdk_single_joint_step.py --execute --channel can_arm_a --firmware v112 --joint 1 --delta-deg 2 --speed-percent 10
.venv/nero-sdk/bin/python examples/nero_sdk_single_joint_step.py --channel can_arm_b --firmware v112 --joint 1 --delta-deg 2 --speed-percent 5
.venv/nero-sdk/bin/python examples/nero_sdk_single_joint_step.py --execute --channel can_arm_b --firmware v112 --joint 1 --delta-deg 2 --speed-percent 5
NERO_CONTAINER_NAME=nero-humble-s10-ros-tool bash scripts/run_humble_container.sh python3 /workspace/nero/scripts/ros_single_joint_step.py --namespace arm_a --joint joint1 --delta-deg 2
NERO_CONTAINER_NAME=nero-humble-s10-ros-tool bash scripts/run_humble_container.sh python3 /workspace/nero/scripts/ros_single_joint_step.py --execute --namespace arm_a --joint joint1 --delta-deg 2
NERO_CONTAINER_NAME=nero-humble-s10-ros-tool bash scripts/run_humble_container.sh python3 /workspace/nero/scripts/ros_single_joint_step.py --namespace arm_b --joint joint1 --delta-deg 2
NERO_CONTAINER_NAME=nero-humble-s10-ros-tool bash scripts/run_humble_container.sh python3 /workspace/nero/scripts/ros_single_joint_step.py --execute --namespace arm_b --joint joint1 --delta-deg 2
bash -n scripts/*.sh
python3 -m py_compile examples/nero_read_state.py examples/nero_sdk_single_joint_step.py scripts/ros_single_joint_step.py
```

## 2026-06-26

- 完成 S11 双臂实验基线：定义 `lab_world`，记录 Arm A/Arm B 底座测量与照片证据，修正静态 TF 目标为 `lab_world -> arm_*/world`，并通过 RViz 双臂布局验证。
- 关闭 S11：接受静态 TF 值，保存 `docs/evidence/pics/S11_RViz_accepted_dual_arm_layout.png`，post-TF 快照 `docs/evidence/ros_snapshots/20260626_055339/` 通过，X11 权限恢复为仅本地用户。
- 完成 S12 控制隔离：Arm A `joint1 +30 deg` 和 Arm B `joint1 -30 deg` 分别单臂主动、另一臂被动监控，均完成 dry-run、执行、回位和 post-motion 快照。
- 验证 S12 被动臂隔离效果：Arm A 动作时 Arm B 最大偏差约 `0.005 deg`，Arm B 动作时 Arm A 最大偏差约 `0.008 deg`，均远低于 `1.0 deg` 阈值。
- 准备并执行 S13 低风险双臂原语：首次 Arm A `+30 deg` / Arm B `-30 deg` 数值执行成功但可见方向语义不符合预期；修正为 Arm A `+30 deg` / Arm B `+30 deg` 后，执行核心通过。
- S13 最终快照首次未验收：`docs/evidence/ros_snapshots/20260626_093414/` 状态健康但 joint-state 约 `400 Hz`，判断存在重复 publisher，S13 暂不关闭。

关键命令：

```bash
bash scripts/run_humble_container.sh bash /workspace/nero/scripts/launch_dual_ros_readonly.sh
NERO_CONTAINER_NAME=nero-humble-s11-static-tf bash scripts/run_humble_container.sh bash /workspace/nero/scripts/publish_s11_static_tf_candidate.sh
NERO_CONTAINER_NAME=nero-humble-s11-rviz bash scripts/run_humble_container.sh --allow-xhost bash /workspace/nero/scripts/launch_s11_dual_model_view.sh
NERO_CONTAINER_NAME=nero-humble-s11-snapshot bash scripts/run_humble_container.sh bash /workspace/nero/scripts/snapshot_ros_readonly_state.sh
NERO_CONTAINER_NAME=nero-humble-s12-arm-a bash scripts/run_humble_container.sh bash /workspace/nero/scripts/launch_s12_isolation_pair.sh arm_a
NERO_CONTAINER_NAME=nero-humble-s12-tool bash scripts/run_humble_container.sh python3 /workspace/nero/scripts/ros_s12_isolation_step.py --target arm_a --joint joint1 --delta-deg 30
NERO_CONTAINER_NAME=nero-humble-s12-tool bash scripts/run_humble_container.sh python3 /workspace/nero/scripts/ros_s12_isolation_step.py --execute --target arm_a --joint joint1 --delta-deg 30
NERO_CONTAINER_NAME=nero-humble-s12-arm-b bash scripts/run_humble_container.sh bash /workspace/nero/scripts/launch_s12_isolation_pair.sh arm_b
NERO_CONTAINER_NAME=nero-humble-s12-tool bash scripts/run_humble_container.sh python3 /workspace/nero/scripts/ros_s12_isolation_step.py --target arm_b --joint joint1 --delta-deg -30
NERO_CONTAINER_NAME=nero-humble-s12-tool bash scripts/run_humble_container.sh python3 /workspace/nero/scripts/ros_s12_isolation_step.py --execute --target arm_b --joint joint1 --delta-deg -30
NERO_CONTAINER_NAME=nero-humble-s13-dual-active bash scripts/run_humble_container.sh bash /workspace/nero/scripts/launch_s13_dual_active_pair.sh
NERO_CONTAINER_NAME=nero-humble-s13-tool bash scripts/run_humble_container.sh python3 /workspace/nero/scripts/ros_s13_dual_joint_step.py --joint joint1 --arm-a-delta-deg 30 --arm-b-delta-deg -30
NERO_CONTAINER_NAME=nero-humble-s13-tool bash scripts/run_humble_container.sh python3 /workspace/nero/scripts/ros_s13_dual_joint_step.py --execute --joint joint1 --arm-a-delta-deg 30 --arm-b-delta-deg -30
NERO_CONTAINER_NAME=nero-humble-s13-tool bash scripts/run_humble_container.sh python3 /workspace/nero/scripts/ros_s13_dual_joint_step.py --joint joint1 --arm-a-delta-deg 30 --arm-b-delta-deg 30
NERO_CONTAINER_NAME=nero-humble-s13-tool bash scripts/run_humble_container.sh python3 /workspace/nero/scripts/ros_s13_dual_joint_step.py --execute --joint joint1 --arm-a-delta-deg 30 --arm-b-delta-deg 30
bash -n scripts/*.sh
python3 -m py_compile scripts/ros_s12_isolation_step.py scripts/ros_s13_dual_joint_step.py
```

## 2026-06-27 至 2026-06-28

- `docs` 中未检索到这两天的新阶段记录或验收快照；当前工作记录从 6 月 26 日直接延续到 6 月 29 日的 S13 最终复验。

## 2026-06-29

- 完成 S13 最终复验：清理额外 ROS driver/terminal 状态，确认 `/arm_a/feedback/joint_states` 和 `/arm_b/feedback/joint_states` 的 publisher count 均为 `1`。
- 记录中间快照 `docs/evidence/ros_snapshots/20260629_043358/` 为不验收：A/B 状态健康，但 joint-state 仍约 `400 Hz`，重复 publisher 问题未完全清除。
- 接受最终快照 `docs/evidence/ros_snapshots/20260629_043441/`：`Failed capture commands: 0`，A/B joint-state 恢复约 `200 Hz`，A/B `err_status: 0`，无 joint-limit 和 joint-communication 异常。
- 正式关闭 S13：接受的低风险双臂关节空间原语为 Arm A `joint1 +30 deg`、Arm B `joint1 +30 deg`；Arm A `+30 deg` / Arm B `-30 deg` 保留为被拒绝的世界方向语义假设。
- 路线进入 S14 末端执行器预安装评审；在 S14.0 记录机械、电气、CAN/ID、负载/TCP、URDF/ROS 和 no-motion 只读决策前，不安装、不上电、不配置、不驱动灵巧手。

关键命令：

```bash
ros2 topic info /arm_a/feedback/joint_states
ros2 topic info /arm_b/feedback/joint_states
bash scripts/run_humble_container.sh bash /workspace/nero/scripts/launch_dual_ros_readonly.sh
NERO_CONTAINER_NAME=nero-humble-s13-final-snapshot bash scripts/run_humble_container.sh bash /workspace/nero/scripts/snapshot_ros_readonly_state.sh
bash -n scripts/*.sh
python3 -m py_compile scripts/ros_s13_dual_joint_step.py
```
